name: test project
on: push
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Get Code
              uses: actions/checkout@v3
            - name: Run Tests
              run: |
                npm test 
                echo "All Tests Ran Successfully"
